BYOND_MAJOR_VERSION=516
BYOND_MINOR_VERSION=1667

# SpacemanDMM reference: branch, tag, or commit SHA
# For branches, the installer will resolve to the latest commit hash for caching.
SPACEMAN_DMM_REF=master

# See: goonstation/rust-g - is the tag for the release
RUST_G_VERSION=v3.6.0-G

NODE_VERSION=22

# Python version for mapmerge and other tools
PYTHON_VERSION=3.9.0
