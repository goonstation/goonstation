// List of variables that should not be copied from a bioeffect instance when saving to the genetics console,
// creating injectors, or otherwise storing the gene.
#define GENETICS_COPY_VARIABLE_BLACKLIST list("owner", "holder", "dnaBlocks", "timeLeft", "degrade_after")
