#!/bin/sh
if [ -z "$TG_SKIP_TGUI_HOOK" ]; then
	tools/bootstrap/python -m hooks.tgui_hook pre-commit "$@"
	status=$?
	if [ $status -ne 0 ]; then
		exit $status
	fi
fi

if [ -z "$TG_SKIP_MAP_MERGE" ]; then
	exec tools/bootstrap/python -m mapmerge2.precommit
fi

exit 0
