import { useBackend } from '../backend';
import { Button, Collapsible, Box, Section } from '../components';
import { Window } from '../layouts';

export const CuttingHair = (props, context) => {
  const { act, data } = context;
  const { hair_styles, cut_or_shave, preview } = data;

  return (
    <Window>
      <Window.Content>
        <p>yewwwwwwwwwws</p>
      </Window.Content>
    </Window>
  );
};
